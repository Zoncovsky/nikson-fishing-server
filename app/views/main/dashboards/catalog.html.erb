<div class="nav-scroller py-1 mb-3 border-bottom bg-light">
  <nav class="nav nav-underline justify-content-between ms-4 me-4">
    <% @main_categories.each do |category| %>
      <%= link_to catalog_path(category_id: category.id), class: 'nav-item nav-link link-body-emphasis', style: 'color: #000000;', data: { category_id: category.id } do %>
        <%= category.name %>
      <% end %>
    <% end %>
  </nav>
</div>
<div class="container">
  <div class="album py-5 bg-body-tertiary bg-primary text-dark bg-opacity-75">
    <div class="container">
      <div class="row row-cols-1 row-cols-sm-2 row-cols-md-3 g-3">
        <% @products.each do |ppr| %>
          <div class="col">
            <div class="card shadow-sm h-100">
              <%= image_tag (ppr.images.first), class: 'bd-placeholder-img card-img-top img-fluid', style: 'height: 400px; object-fit: cover;' %>
              <div class="card-body d-flex flex-column">
                <p class="card-text text-center"><%= ppr.name %></p>
                <p class="card-text"><%= ppr.description %></p>
                <p class="card-text fw-bold"><%= I18n.t('product_page.price') %> <%= ppr.price %> <%= I18n.t('main_page.currency') %></p>
                <div class="d-flex justify-content-between align-items-center mt-auto">
                  <div class="btn-group">
                    <%= link_to t('main_page.view_details'), main_product_path(ppr), class: 'btn btn-sm btn-outline-primary' %>
                  </div>
                  <button type="button" class="btn btn-outline-primary btn-sm"><i class="bi bi-cart"></i></button>
                </div>
              </div>
            </div>
          </div>
        <% end %>
      </div>
    </div>
  </div>
</div>
